/**
 * {@code MockMailSender} — simulates mail delivery (including randomized failures) so the retry
 * pipeline can be exercised without a real mail transport.
 */
package com.company.mailing_service.infrastructure.service.mock;
